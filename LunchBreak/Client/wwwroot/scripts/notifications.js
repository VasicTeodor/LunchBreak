window.AlertSuccess = (messaage) => {
    alertify.success(messaage);
};

window.AlertError = (message) => {
    alertify.error(message);
};

window.AlertConfirm = (message) => {
    alertify.confirm(message);
};

window.Alert = (messaage) => {
    alertify.alert(messaage);
};